import { authenticateInstallation, ingestNativeBatch, type NativeHealthSample } from "@/lib/native-health";
export async function POST(request:Request){const installation=await authenticateInstallation(request.headers.get("authorization"));if(!installation)return Response.json({error:"Invalid companion token"},{status:401});try{const body=await request.json() as {idempotencyKey:string;cursor?:string;samples:NativeHealthSample[]};return Response.json(await ingestNativeBatch(installation,body));}catch(error){return Response.json({error:error instanceof Error?error.message:"Sync failed"},{status:400});}}
